<%- include('partials/header') %>

<div class="title">    
    <h1>Articles</h1>
</div>

<div class="row">

    <% posts.forEach(post => { %>
        <div class="mt-4 col-6">
            
            <div class="card">
                <div class="card-header">
                    <h2><%= post.title %></h2>
                </div>
                <div class="card-body">
                    <p>By <%= post.auth %></p>
                    <p><%= post.date.toLocaleString('en-GB') %></p>
                    <p><%= post.content.substring(0, 100) %>...</p>
                </div>
                <div class="card-footer">
                    <a href="/blog/<%- post._id %> " class="btn btn-sm btn-st">Read more...</a>
                </div>
        </div>
    </div>
    
    <% }) %>
</div>

<%- include('partials/footer') %> 