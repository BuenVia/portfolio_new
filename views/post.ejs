<%- include('partials/header') %> 

<div class="mt-4">
    <div class="card">
        <div class="card-header">
            <h3><%= art.title %></h3>
        </div>
        <div class="card-body">
            <%= art.content %>
        </div>
        <div class="card-footer">
            <p>Posted by: <%= art.auth %></p>
            <p>Date: <%= art.date.toLocaleString('en-GB') %></p>
        </div>
    </div>
</div>

<%- include('partials/footer') %> 